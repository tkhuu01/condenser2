[tools]
postgres = "18.3"

[tasks.qa]
description = "Format, sort imports, and lint"
run = [
    "uv run ruff format src/",
    "uv run ruff check --select I --fix src/",
    "uv run ruff check src/"
]
