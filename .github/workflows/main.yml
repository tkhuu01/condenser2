name: Main CI
on: push
jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ruff Format
        uses: astral-sh/ruff-action@v3.5.1
        with:
          args: 'format --check'
      - name: Ruff Lint
        uses: astral-sh/ruff-action@v3.5.1
